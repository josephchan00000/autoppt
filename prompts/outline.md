# Stage 5 — 投影片文案潤飾

`05_outline.py` 已經把 digest + evidence 合成 `work/05_deck.json` 骨架，
並算好頁數預算與版面分派。**你的工作是潤飾文案，不是重排版面。**

不要改動：`id`、`layout`、`kind`、`ch_id`、`sources[].url`。
可以改動：`title`、`subtitle`、`body`、`sources[].label`、`duration_sec`。

## 【標題】

- 主標一律寫成「**有結論的句子**」，不是名詞標籤
  - ✗「風險管理」
  - ✓「風險預算該分配在部位，不在情緒」
  - ✗「資產配置的重要性」
  - ✓「配置決定九成績效，選股貢獻扣成本後歸零」
- 主標 ≤ **14 字**；副標 ≤ **21 字**，副標負責補「為什麼／所以呢」
  （母片「內頁1-1」主標框是 36pt 單行、寬 6,840,760 EMU，約放得下 14 個全形字。超過會折成兩行壓到副標上。）
- 同一章內的主標不要重複句型，連續三頁都用「X 才是 Y」會很呆

## 【內文】

- **動筆前先問：這頁非得條列不可嗎？** 是機制就畫 `flow`、是對照就畫 `table`
  或 `split`、是時間刻度就畫 `timeline`。判斷表在 `prompts/visuals.md` 開頭
- 真的要條列：每頁第一層要點 **最多 3 條**，每條 ≤ **40 字**，不寫完整句號結尾的長句。
  3 是上限不是目標，2 條講得清楚就不要湊
- **連續最多 2 頁條列**，第 3 頁一定要是視覺頁、引言頁或大數字頁
- **全書視覺頁至少佔四成**（`08_qa.py` 的「節奏」檢查會抓）
- 層級**最多 2 層**（level 0 與 level 1），不准出現第 3 層
- 每頁總字數 ≤ **160 字**（4:3 版面，24pt 內文，超過就爆版）
- 每頁**至少有一個「具體物」**：數字、年份、案例名、金額、比例。
  全部都是抽象概念的頁面一律重寫。
  - ✗「市場情緒會影響投資決策，投資人應保持理性」
  - ✓「2020/3 台股單月跌 14%，散戶淨賣超 380 億，三個月後全數回補」
- 第二層（level 1）只在需要舉證或補數字時使用，不要每條都掛一個第二層

## 【視覺頁】← 另讀 prompts/visuals.md

視覺頁不是「把數字畫成折線圖」。判斷順序是
**書中原圖 > 外部機構原圖 > 流程圖／時間軸／對照表／雙欄 > 引言頁與大數字頁 >
自己畫的圖表**，完整規則與 deck.json 欄位寫法見 `prompts/visuals.md`。

視覺頁的字數上限**看它走哪個版面**：
`image` / `table` / `split` 走內頁2-1，與內容頁相同（主標 ≤14、副標 ≤21）；
`flow` / `timeline` / `chart` 走空白內頁，比較寬（主標 ≤20、副標 ≤30）。

## 【資料來源】

- 每一頁都要有 `sources`（封面／頁籤／結尾除外）
- 書內容寫「《書名》p.XX」
- 外部資料寫「機構名（YYYY/MM）」
- 外部資料**同時保留 `url` 進 deck.json**，供 QA 檢查，但投影片上只印機構名與日期
- 不要把 URL 寫進 `label`

## 【語氣】← 這次要用哪一種，看 deck.json

`work/05_deck.json` 的 `meta.tone_directive_slide` 就是這次選定的語氣規則，
**動筆之前先讀它**，整份稿子照著寫。三種語氣（專業嚴謹／輕鬆口語／說故事）
對主標句型、可不可以用轉折詞、數字要怎麼呈現的要求都不一樣。

語氣規則的原文在 `config/project.yaml` 的 `tone_presets`，要改措辭改那裡。

## 【語言】

- 繁體中文為主，專有名詞保留英文原文，首次出現寫「中文（English）」
- 不使用「賦能、抓手、閉環、顆粒度、打法、復盤、落地性、用戶、信息、質量」
  這類對岸用語

## 【各種頁型的文案重點】

| kind | 重點 |
|---|---|
| `cover` | 主標＝書名；副標＝這本書的一句話定位（不是副書名） |
| `toc` | 用章節群組名，不要照抄原書目錄；每條要看得出「這段要回答什麼問題」 |
| `divider` | 只有章節名。不要加內文 |
| `content` | 主標＝結論句；body 3–5 條；至少一個具體物 |
| `chart` | 主標＝這張圖要說的結論（不是圖名）；副標補資料範圍與單位；body ≤ 2 條 |
| `chart` + `image` | 書中原圖或外部機構原圖；走內頁2-1，body 留空讓圖自己講 |
| `chart` + `table` / `split` | 原生圖形，走內頁2-1；body 會畫成圖下方一行結語 |
| `chart` + `flow` / `timeline` | 原生圖形，走空白內頁（要完整高度） |
| `chart` + `quote` / `stat` | 引言頁／大數字頁；不畫頁面標題，`duration_sec` 給 20 秒 |
| `closing` | 一句話收束全書 + Q&A |

特別注意三種頁：
- **「為什麼讀這本書」**：要講「這本書解決了我們手上哪個具體問題」，不要寫書評
- **「我的異議」**：從各章 `counterpoint` 綜合，要有立場，不要寫「本書仍有參考價值」
- **「可落地的行動」**：每條都要是「明天就能做」的動作，不是原則

## 逐頁檢查表（潤飾完自己跑一次）

- [ ] 有照 `meta.tone_directive_slide` 的語氣寫？
- [ ] 主標是句子不是名詞？
- [ ] 主標 ≤14 字、副標 ≤21 字？
- [ ] body ≤3 條、每條 ≤40 字、總字數 ≤160？
- [ ] 這頁能不能改畫成 flow / table / split / timeline？
- [ ] 前面兩頁是不是也是條列頁？
- [ ] 沒有第 3 層？
- [ ] 這頁有數字／年份／案例名嗎？
- [ ] sources 有填，且外部來源格式是「機構名（YYYY/MM）」？
- [ ] 沒有對岸用語？

改完存回 `work/05_deck.json`，然後跑 `python scripts/08_qa.py --check-deck` 驗證。
