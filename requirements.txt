# 復華風格「專業書分享」簡報自動化 pipeline
# Python 3.10+  /  pip install -r requirements.txt

# --- 投影片與文件產出 ---
python-pptx==1.0.2
python-docx>=1.1.2

# --- 書檔解析 ---
pymupdf>=1.24.0          # PDF 文字層抽取 + 書籤 + 轉圖（OCR 前置）
ebooklib>=0.18           # EPUB
beautifulsoup4>=4.12.0   # EPUB HTML 解析
lxml>=5.0.0

# --- 設定與工具 ---
pyyaml>=6.0
pillow>=10.0.0
rapidfuzz>=3.6.0         # 頁眉頁腳偵測、章節標題模糊比對

# --- 圖表 ---
matplotlib>=3.8.0
pandas>=2.0.0

# --- OCR（僅掃描版 PDF 需要，另需系統安裝 tesseract）---
pytesseract>=0.3.10

# --- QA 連結檢查 ---
requests>=2.31.0

# --- 影片頁 QR code ---
qrcode>=8.0

# --- 寫回設定檔（04_research --accept-video、tools/repace_deck）---
ruamel.yaml>=0.18        # round-trip 寫 project.yaml，保留註解
